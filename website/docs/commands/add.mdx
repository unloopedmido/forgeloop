---
sidebar_position: 2
title: forgeloop add
description: Add command, event, modal, button, or select-menu handlers for modular and advanced projects.
---

import { FlagTable } from '@site/src/components/docs/FlagTable';

## Usage

```bash
forgeloop add command <name> [--description "..."]
forgeloop add event <eventName> [--once] [--on]
forgeloop add modal [--custom-id <id>] [<customId>]
forgeloop add button [--custom-id <id>] [<customId>]
forgeloop add select-menu [--custom-id <id>] [<customId>]
```

**Basic** preset projects **cannot** use `add` — handlers are inline. Error message:

> This ForgeLoop project uses the "basic" shape, so commands and events stay inline in index.ts/js. Switch to "modular" or "advanced" to use handler generators.

Wrong subcommand:

> Unsupported add target "…". Try "command", "event", "modal", "button", or "select-menu".

Missing name (non-interactive):

`Usage: forgeloop add command <name> [--description "..."]`

`Usage: forgeloop add event <eventName> [--once]`

`Usage: forgeloop add <modal|button|select-menu> [--custom-id <id>] [<customId>]`

## Command generator

- **Name:** letters, numbers, hyphens, underscores — stored **lowercase**.
- **Description:** optional; default stub text in the `SlashCommandBuilder` if empty.

## Event generator

- **Name:** must be a valid Discord.js event identifier (e.g. `messageCreate`).
- **`--once` / `--on`:** control `client.once` vs `client.on`. Default: **`once` for `clientReady`**, otherwise **`on`** if neither flag is set.

## Interaction generators

- **Targets:** `modal`, `button`, `select-menu`.
- **`customId`:** required (positional or `--custom-id`), max length 100.
- Files are generated under `src/interactions`:
  - `modals/<slug>.ts|js`
  - `buttons/<slug>.ts|js`
  - `selectMenus/<slug>.ts|js`

## Flags

<FlagTable
  rows={[
    {
      flag: '--dir',
      type: 'string',
      defaultValue: 'cwd',
      description: 'Project root containing ForgeLoop config.',
    },
    {
      flag: '--description',
      type: 'string',
      description: 'Only for add command — slash command description.',
    },
    {
      flag: '--once',
      type: 'boolean',
      description: 'Only for add event — register with client.once.',
    },
    {
      flag: '--on',
      type: 'boolean',
      description: 'Only for add event — register with client.on.',
    },
    {
      flag: '--custom-id',
      type: 'string',
      description: 'Only for modal/button/select-menu — custom interaction ID.',
    },
  ]}
/>

## Files written

- Command: `src/commands/<name>.ts` or `.js` (from manifest paths).
- Event: `src/events/<eventName>.ts` or `.js`.
- Modal: `src/interactions/modals/<slug>.ts` or `.js`.
- Button: `src/interactions/buttons/<slug>.ts` or `.js`.
- Select menu: `src/interactions/selectMenus/<slug>.ts` or `.js`.
